{
  "name": "oficiospro-mcp-server",
  "version": "1.0.0",
  "description": "Read-only MCP server for the OficiosPro admin API (model health, specialists, conversion events, CRM, leads).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "oficiospro-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
